# 待办事项

> 按 AGENTS 约定记录改进待办；做完的挪到底部归档区。

## 进行中

- [ ] **审计遗留（2026-09-20）**：全仓审计八项已修复入库（编译 0 错 0 警、94/94 测试过）；验收残留缺口与两项已拍板大项（**Gamma→Linear 迁移 + 全量重锚定**、**SlimePbf/SlimePbfMesh 公共内核抽取**）全部收录在 [审计遗留清单.md](审计遗留清单.md)，按 P1→P4 顺序处理；构建验收 exe 排在 Linear 迁移完成之后，构建时照清单底部"实机验收清单"五项验收
- [ ] **空闲帧率节流**：桌宠 7×24 常驻后台，当前全速渲染（全工程无 targetFrameRate/OnDemandRendering 配置）；空闲（无交互且宠物静息）时降帧省电、交互/拖拽时恢复全速。候选手段 `Application.targetFrameRate` 或 `OnDemandRendering.renderFrameRate`（2026-09-18 结构重构盘点发现，属行为改动，独立提交）。注：duplication 主路径已自带变化驱动等待（桌面静止时抓屏线程挂起零开销，2026-09-19），本条收窄为渲染帧率侧
- [ ] **本机 Desktop Duplication 不可用的环境整治**（2026-09-19 实测坐实）：本机 DXGI 枚举形态异常——DISPLAY1 唯一输出挂在 NVIDIA 适配器、Intel 五个重复枚举条目全无输出（远程控制/模拟器的三个虚拟显示器驱动嫌疑：向日葵 OrayIddDriver、MuMu、GameViewer），而混合显卡上 DuplicateOutput 必须建在 DWM 合成的 GPU 上 → NVIDIA 上一律 UNSUPPORTED。代码侧已按最佳实践落地：duplication 主路径（全适配器扫描逐个试复制）+ BitBlt 工作线程回退 + 路径日志；若卸载/禁用虚拟显卡驱动后 duplication 可用，GPU 直拷 + 变化驱动的性能收益自动激活（届时需实机复验 duplication 路径的 WDA 抓屏隐形语义——官方文档与 BitBlt 同语义，未实机验证）
- [ ] **睡眠/休眠实测复验**（2026-09-19 代码侧已自愈化）：duplication 路径 AccessLost 自动重建覆盖睡眠唤醒/锁屏/全屏切换；BitBlt 回退无状态天然免疫；心跳看门狗 10s+5s 复核兜底误杀。剩用户日常合盖唤醒实测：双进程存活、抓屏恢复出图、crashguard.log 无误杀记录
- [ ] **抓屏线程按需降频**（2026-09-19，小项，仅 BitBlt 回退路径需要）：duplication 主路径变化驱动已免轮询；BitBlt 回退仍无条件 ~30fps 全屏抓（本机实测偶发 110ms 级 DWM 阻塞，已被工作线程化隔离），可按交互活跃度降频省电
- [ ] **卡死修复的实机验证**：运行 `F:/Downloads/PetSpike/PetSpike.exe`，重点确认四件事——①**退出必死**（托盘"退出"/ESC 后进程立刻消失，不再需要任务管理器）；②**穿透正常**（鼠标移到宠物不透明区域上能抓；移到空白处点击落到桌面）；③**无开场画面**（窗口在场景就绪前不出现，也不会整轮不出现）；④挂起是否复现（对照 `C:/ProgramData/Microsoft/Windows/WER/ReportArchive/AppHang_Pet*`，修复前 09-12 17:16~09-13 02:51 共 8 次）
- [ ] **启动画面屏蔽去留定夺**（2026-09-19 小窗口方案已实施）：PlayerSettings 已改 Windowed + 200×120 默认分辨率，SplashHider 藏窗竞态漏光从"全屏灰闪"缩小到"小灰点"；根治选项为升 Unity 6（个人版可合法关 splash，2024-09 官宣），见下方评估条目
- [ ] **双进程启动显示同步**（2026-09-19 诊断）：错峰初始化保留（5s 防抢显卡），显示放行改握手——物种侧初始化完不自行显示、等玻璃 `show` 命令；玻璃侧探到物种窗口标题后同时放行两边（复用 `species_commands.json` 通道 + 现有窗口探测）；兜底超时 ~20s 玻璃自己显示
- [ ] **Unity 6 升级评估**（splash 根治 + 求职叙事加分）：Unity 6（6000.0）起个人版可在 Player Settings 合法关闭 Made with Unity 启动画面（2024-09 官宣、2024-10 随 Unity 6 生效）；本项目包依赖干净（UniWinC + Test Framework），迁移风险中等
- [ ] **生命感版交付验收**：首次启动有 4 秒引导提示；静息会呼吸、拖动时身体倾斜、落地按冲击压扁回弹、戳一下会弹；托盘"设置/退出"、位置记忆照旧
- [ ] 快照对比存档：`C:/Users/fanbo/AppData/Local/Temp/pet-snapshot/combined_compare.png`（左原版/右 PBF，历史资料，不再更新）

## 排队中

- [ ] **卡死若仍复现，按"相位标记 + 心跳"定位**：在 `Pump`/`TrackPopupMenu`、`Shell_NotifyIcon` 前后写带 flush 的日志，外加后台心跳线程（>2 秒无帧就记下最后一个相位），用 `-logFile -` 跑。本轮排查吃过"Player.log 带缓冲、强杀时尾部全丢"的亏（见归档教训）
- [ ] 托盘菜单 Win32 细节收口：菜单宿主仍是 `HWND_MESSAGE` 消息窗口（写过探针实测 `SetForegroundWindow` 能成功，故未改），但 `TrackPopupMenu` 期间主循环停摆属固有行为——可考虑改为非模态自绘菜单
- [ ] **窗口跟随宠物包围盒**（取代全屏透明覆盖层）：把失败影响面从"遮挡整个桌面"降到"一小块"；代价是窗口需随宠物移动（SetWindowPos）与多显示器处理。**注（2026-09-21 核对）**：本条此前写着"V9 已实现同款机制（ApplyWindowPlacement + FitToMonitor 注入）"，实际代码里 `LiquidGlassController.ApplyWindowPlacement` 已不存在——V9 当初做过包围盒收缩形态，后来**废弃并退回全屏覆盖层**（`PetWindowSetup.cs:55` 注释"收缩形态已废弃"），故本项等于**未开工**，要做需从头实现
- [ ] 更早版本的场景化保留（V1 贴图刚体版 / V1.5 轮廓软体版——代码在 git 历史，需按 Versions/ 约定 resurrect 成独立场景）
- [ ] ~~分裂/融合（PBF 物理断开 + 聚类回流，参照 Unity_Slime 连通域方案）~~（2026-09-18 用户拍板：分裂/融合整体不再要做，随 V4 版本一并彻底删除）
- [ ] **V9 液态玻璃收尾（可选）**：混合 DPI/多屏抓屏坐标适配（当前按主屏、无 DPI 缩放交付）；全屏 BitBlt 换 DXGI Desktop Duplication 提帧率；多只拖拽上手确认（渲染已截图验证，拖拽逻辑与单只同源）
- [ ] 液态玻璃进阶（可选）：多只相邻融合的 CPU 命中判定与视觉对齐（当前逐只独立判定，融合区略有出入）
- [ ] **玻璃线生命感的实机手感验收**（2026-09-21 代码完成，待用户上手）：拖拽甩出抛物线与反弹、落定悬浮（停稳后关重力）、静息呼吸、落地挤压回弹、戳一下弹、多只呼吸错相位；另需确认拖拽中的倾斜方向与幅度是否合口味。离屏定点图（挤压/倾斜）在 `%TEMP%/pet-snapshot/liquidglass_life_*.png`，可先用它看形变方向
- [ ] 音效（低优先）
- [ ] 编辑器 Play 体验（Game 视图切换/黑底问题，不影响 exe 交付链路）
- [ ] 多显示器支持验证（shouldFitMonitor 只适配主屏）
- [ ] `Assets/Scenes/Versions/` 与 `ProjectSettings/EditorBuildSettings.asset` 路径不一致：后者仍写着 `V2SplitFusion`/`V9PbfHover` 等已不存在的路径（重跑场景生成器即可对齐）
- [ ] `Assets/Scripts/Display/` 与构建产物里的 `TransparentPet.Display.dll` 来历未明（源码树里没有对应的 asmdef 与其源文件），需清理或补文档
- [ ] 首个正式版打包时把工程 Temp/ 加入 .gitignore 或清理（历史上出现过 Unity 僵尸进程持锁导致无法构建，处置：复制工程到新路径构建）
- [ ] **SlimePbfMesh 的 SquashPulse 疑似永不触发**（2026-09-18 补碎裂版测试时离线实测发现）：`StepFrame` 的触发条件 `prevVelY>300 && Velocity.y<100` 在帧粒度比较，而落地减速被密度约束分摊在多帧上、从不发生同帧跨越——落差 344~2044px 实测峰值全为 0。属观感行为改动，修复需用户拍板并走视觉锚定验收（Jelly 版 SlimePbf 同款逻辑，需一并确认）
- [ ] SlimePbfMesh 轻微问题（2026-09-18 测试发现）：头注释"粒子少（~190）"过时（半宽 88 实测 247）；`MoveGrab` 滑窗对 `dms≤1e-3` 的段跳过求和却仍除以满额段数，不均匀采样时会低估抛速（边缘情形）

## 归档

- [x] 2026-09-21 P1 玻璃线补齐「手感 + 生命感」（用户拍板方向：**可甩可抛 + 落定悬浮**、生命感走**变换级**）。背景：交付默认的液态玻璃线此前是四条线里唯一没有抛射的（松手不给抛速、静息零动效），而 README 头牌玩法是「拖拽抛掷」。
  - **抛射**（`3a13c11`）：`ThrowPhysics` 拆出显式半尺寸重载 `Step(...halfW, bottomH)`（玻璃轮廓来自 SDF，没有可折算的贴图比例；补等价性单测锁住两条路径一致）。新增 `GlassSlimeMotion`（纯逻辑可测）：拖拽/抛射/落定悬浮三态，碰撞半尺寸按 `LiquidGlassSlimeSdf` 常量换算（全宽 320 → 半宽 160、底 110、顶 92.4，上下不对称）；接地且低速持续 0.35s 即关掉抛射态冻结位置——即「落定悬浮」，顺带根治 `ThrowPhysics`「接地后微幅反弹不主动终止」对常驻玻璃会永久微抖的问题。控制器接线：拖拽改走 `DragMove` 喂速度样本（松手才有抛速）、松手补「确实在被拖」守卫（对齐当天修的进程级松手串扰）、`StepMotions` 只推进飞行中的只、`ClampToWorkArea` 改按 SDF 轮廓半尺寸夹紧（旧实现固定 60px 内缩，而缩放 2.5 倍时半宽达 400px，玻璃会大半挂在屏幕外）。静置玻璃不受重力 → 启动构图与旧版一致。
  - **生命感**（`7328e1f`）：`PetLifeMath` 由 `Pet/Textured` 移到 `Pet/Common`（本就是跨物种共享定位）；`LiquidGlass.shader` 新增每槽 `_ItemShape`（xy = 非等比缩放、z = 绕中心旋转弧度）；新增 `GlassSlimeLife`（纯逻辑可测）用 `LifeTuning` 常量推进呼吸/浮沉/挤压弹簧/拖拽倾角，多只按槽位错相位；控制器 `TickLife` 每帧推进，推槽位时叠加 `OffsetY` 并推 `_ItemShape`；戳击判定阈值同贴图线（6px/0.35s）；落地冲击在 `StepMotions` 当场注入（落定后不再 Step，留到 `TickLife` 读会反复注入）。**分层不变**：生命感只作用于渲染，逻辑位置、CPU 命中判定与碰撞边界一律用未变形的静息轮廓。
  - **验证**：119/119 EditMode 通过、0 编译警告；反证（撤掉修复跑回归）在投掷串扰那条已做。shader 经**真实渲染**验证（batchmode 编译不触发 D3D 编译，AGENTS 记过此坑）：`LiquidGlassSnapshot` 新增 identity/squash/tilt 三张同底定点图，identity 与同参数的改造前 gradient 图 **MD5 相同**（aeaa3fa4…），挤压图横向涨纵向扁且平底保留，-10° 倾斜图顶部朝运动方向倾倒、与 `PetLifeVisual` 同向。
  - **遗留**：实机手感验收未做（见「排队中」）；若看完想升级为 SDF 真变形，需动 shader 与 CPU 命中共用的那套贝塞尔控制点，两处必须同步。

- [x] 2026-09-21 投掷串扰修复（用户报告"投掷相关代码会错误地影响除被投掷者以外的其他史莱姆"）：根因两处叠加——①`SvgPetController.HandleInput` 的松手分支是裸的 `if (Input.GetMouseButtonUp(0))`，而 `Input` 是**进程级**输入、同屏每只贴图史莱姆都会各收到一次，于是松手会广播到所有只（对比 `PetController`/`MeshPetController` 都带 `&& sim.IsGrabbed` 守卫；原文里的 `wasDragging` 只参与戳判定，没起守卫作用）；②`ThrowPhysics.DragEnd` 在 `IsDragging == false` 时照样按缓冲均值起抛，而速度缓冲只在 `DragBegin`/`Reset` 清空 → 甩过一次的实例"终身武装"，此后任何一次松手都用残留样本复飞。症状：甩出 A 后，去点 B 或拖 C，A 会再次起飞；甩过的只数越多连锁越明显。**修复**：`ThrowPhysics.DragEnd` 未在拖拽时整段忽略（对齐 SlimePbf/SlimePbfMesh 的 Release 语义），并在结算后清空速度缓冲；松手分支补 `IsDragging` 守卫。**回归测试 3 项**（`DragEnd_WithoutDragBegin_IsIgnored` / `DragEnd_ClearsVelocityBuffer` / `DragEnd_WhenNotDragging_DoesNotRelaunchWithStaleBuffer`）；临时撤掉修复复跑确认其中 2 项变红（清缓冲项 `Expected 0 But was 1`、复飞项 `Expected 0.0 But was 400.0`），证明测试确实钉住该缺陷而非空转。102/102 EditMode 通过、0 编译警告。**遗留**：`LiquidGlassController` 的松手同样是裸 `GetMouseButtonUp`（当前只置 `dragging = false` 故无害），给玻璃线接抛射时必须一并加守卫，否则会复制同一个坑——已记入下方"排队中"

- [x] 2026-09-21 透明史莱姆闪烁按用户确认归档：现有穿透状态机（`PetWindowSetup` 的样式写入去抖）已解决用户观察到的闪烁，本条关闭；原诊断里的 `WM_NCHITTEST` 方案（零样式翻转零重合成）降级为可选优化，不再作为待办跟踪

- [x] 2026-09-19 设置窗口 + 托盘菜单游戏化 + 小窗口启动（[设计文档](../设计/设置窗口与托盘菜单设计.md)，三开放点用户拍板"都可以"）：①NativeTray 子菜单（MF_POPUP 递归，菜单 id 按叶扁平编号）+ 勾选态 + 左键单击直达动作，FlattenLeaves 纯逻辑 6 项单测；②托盘菜单从 8 项扁平收纳为"召唤>/收回>子菜单 + 设置… + 退出"，左键=打开设置；③SettingsPanel 重做（09-18 回退时删除，用户主动恢复）——IMGUI 自绘经典白对话框（#F0F0F0 底 + 深灰标题栏可拖动 + [×]），四页签：桌宠管理（玻璃/三物种 ●×N 数量行 + [−][+] 走召唤收回事件，物种计数只读 summoned_pets.json）、物理（抛射开关+四滑条）、窗口（置顶/抓屏隐形/自启）、关于；打开时重读 config 工作副本、0.4s 防抖落盘 + ConfigSaved 广播 + `config` 命令热更新物种进程（重发布缩放/抛射/角色事件）；④ESC 模态让位——面板开着时 ESC 只关面板不退出（新增 Core/OverlayState 中转，Platform 不依赖 UI）；⑤重建 LiquidGlassPresence 注册表（Pet↔UI 不直接依赖）；`recall:kind` 按物种收回；⑥小窗口启动（fullscreenMode Windowed + 200×120，SplashHider 竞态漏光从全屏灰闪缩成小灰点）；场景经 GenerateAll 重生成（Glass+7 版本场景装配面板）。94/94 EditMode 测试过（新增 TrayMenuTests 6 项）；exe 已交付 F:/Downloads/TransparentPet/；待用户实机验收：左键开面板、拖动、改缩放 1s 内物种生效、ESC 两级语义、开机灰屏观感

- [x] 2026-09-17 V9 转正发布（用户拍板"变成默认显示 + 多只同屏 + 发布"）：V9 提为 index 0 交付默认（BuildPlayer.ScenePath 同步指 V9）；**多只同屏**（≤3，shader 槽位 + smin 液滴融合，截图验证两只融合成液滴形）；设置面板对齐——新增"液态玻璃"区块（抓屏隐形开关 + 数量 −/+，经 `LiquidGlassPresence`（Core 注册表）中转，避免 Pet↔UI 直接依赖，面板仅液态玻璃场景显示该区块）；多只位置持久化（PetConfig.glassSlimeX/Y 数组，旧单只配置自动迁移）。 exe 已交付 `F:/Downloads/PetSpike/`（PetGallery 展厅共存）。86/86 测试过

- [x] 2026-09-17 V9 真液态玻璃桌面版（折射窗口背后的**真实桌面**）：用户追问"放大镜/OBS 怎么做到的、抓屏能不能不抓自己"后确认路线——**`SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)`**（Win10 2004+，DWM 构建采集帧时跳过本窗口 buffer，本机显示不受影响）+ 窗口收缩为玻璃包围盒 + BitBlt 局部抓屏（`NativeScreenCapture`，BGRA/bottom-up 与 `TextureFormat.BGRA32`/`SetPixelData` 行序零转换对齐）。**spike 教训**：DisplayAffinity 只能由窗口所属进程调用（外部进程 ACCESS_DENIED 错误 5），第一次 spike 走了弯路；layered 窗口上的生效性官方未承诺但实测可用。F11 运行时切换隐形（验收利器：affinity 排除一切采集，机器只能截到"隐形态"，解除后才能截到玻璃本体——本体内容是镜厅自反馈，恰好证明抓屏管线活着）。shader 零改动（`_Bg` 换源）；PetWindowSetup 加 `FitToMonitor` 注入；UniWinC 0.9.8 无无边框属性，`NativeWindowStyles.SetBorderless`（WS_POPUP）补齐。隐形代价已向用户说明：录屏/直播/截图中桌宠不可见。87/87 测试过

- [x] 2026-09-16 V8 液态玻璃版（把 Godot 版液态玻璃演示学过来做成史莱姆形状）：三个 Built-in shader 移植 `modules/effects/`（`LiquidGlassBg` 程序化素材 / `LiquidGlassBlur` 分离式双 pass 高斯 / `LiquidGlass` 主合成：SDF→斯涅尔折射→RGB 色散→LCH 菲涅尔/眩光→抗锯齿）；`LiquidGlassController` 用 RenderTexture+Blit 复刻 Godot SubViewport 管线，可拖拽+位置记忆；命中判定用 `LiquidGlassSlimeSdf`（与 shader 同源贝塞尔控制点，9 项测试）→ PointerHover 悬停自报，玻璃外穿透不挡桌面；棋盘格素材只在玻璃轮廓内可见（适配全屏透明覆盖层）。**踩坑记录**：①ShaderLab Properties 的 `[Header()]` 不容 `&`；②shader 单标量向量构造 `float3(x)`/标量 swizzle `1e-6.xx` 编译期才报错（D3D 编译 lazy，batchmode compile 验证不触发，必须实际渲染）；③编辑器非 Play 下 AddComponent 不触发 Awake，无头快照需幂等 `EnsureInitialized()`；④Godot 4 屏幕坐标 y 向下，sdSlime 平底在 +0.275 端，Unity GL 语义直接用会上下颠倒——SDF 空间统一 y 向下后与 CPU 判定同源。新增 `LiquidGlassSnapshot.CaptureHeadless`（主渲染×2 素材+SDF+法线四图人工验收）；86/86 测试过

- [x] 2026-09-13 卡死四路收口（用户报告"冷不丁还是会卡死"；Windows 事件日志确认 09-12 17:16~09-13 02:51 共 8 次 `AppHangB1`/`AppHangXProcB1`，均判为"主线程在等本进程之外的东西"）：①**退出路径**——`ExitFromTray` 原本先 `Shell_NotifyIcon`（`NIM_DELETE`，同步跨进程调用任务栏）摘图标再 `KillNow`，对方不响应就永不返回、带着全屏置顶窗口赖死（这正是历史上"点了退出进程还赖着不死"的成因，`KillNow` 兜不住"卡在 KillNow 之前"），新增 `HardExit.KillSoon` 延迟强杀引信并在 `Now()`/托盘退出开头点燃；②**去掉每帧 GPU 回读**——`hitTestType` 由 `Opacity` 改 `None` + `isHitTestEnabled=false`（UniWinC 的读屏协程不检查 isHitTestEnabled，只能靠改 hitTestType 关掉），穿透改由新增的 `PointerHover`（Core，帧号显式传入、容忍滞后一帧、6 项单测）驱动，上报方 = 四个宠物控制器（Svg 贴图 alpha / Pbf·Mesh·Split 的 `ContainsPoint`）+ `SettingsPanel`（IMGUI 矩形；其注释原本明确依赖"鼠标落在面板上变可交互"，不自报就会点不动）；③**托盘菜单动作**从窗口过程/`DispatchMessageW` 嵌套栈挪到 `Pump` 顶层执行；④**`FindCurrentProcessTopLevelWindow` 不再单向按可见性过滤**（新增 `requireVisible` 分档 + Unity 窗口类名精确匹配），启动屏蔽"藏起来→再枚举找不到→永远不显示"的根因修掉，隐藏/恢复改为 `HideMainWindowForSplash`/`ReleaseMainWindow` 成对配对（含"藏晚了"竞态保护）。**排查方法教训**：Player.log 带缓冲，进程被强杀时尾部全丢（`Player-prev.log` 只剩 34 行停在正常日志中间），日志尾部毫无线索；真正的证据是 Windows 事件日志（`Application Hang` 1002 + WER `AppHangXProcB1`）与 `crashguard.log` 的启动次数/时间对比（每次挂起后 30 秒~3 分钟必有一次新启动 = 任务管理器杀掉后重开）。77/77 测试通过，exe 已交付 `F:/Downloads/PetSpike/`

- [x] 2026-09-12 失控保护加固（用户反馈"一失控就跟电脑病毒似的"之后）：①**移除构建产物里的崩溃处理器**（`UnityCrashHandler64.exe`）——这是"进程杀不掉"的根因（崩溃时 CrashHandler 挂起进程收集信息导致 TerminateProcess/WMI/NtNativeTerminate 全部 ACCESS_DENIED），移除后实测任务管理器可正常结束进程；②新增 `CrashGuard`（Core）：命名互斥体单实例保护（Unity 的 `forceSingleInstance` 实测无效）+ 独立后台线程启动看门狗（30 秒未进入渲染循环即硬退出，主线程卡死时协程/Update 全停摆，只有独立线程能救）+ AppDomain 未处理异常兜底；③实测：稳定实例下二次启动自动退出（count 保持 1），`crashguard.log` 留痕；已知限制：两次启动几乎同时时，后启动进程可能卡在引擎初始化（应用代码尚未运行，无法拦截），但可被正常终止
- [x] 2026-09-12 构建流程加固（本轮实测踩坑后收口）：①应用图标改由**构建后注入 PE 资源**（`AppIconSetup.InjectIntoExe`：RT_ICON + RT_GROUP_ICON，验证过提取正确）——Unity 的 `PlayerSettings.SetIcons` 在 batchmode 下不落盘（ProjectSettings 恒为空）；②`SceneGenerator` 不再调用 PlayerSettings 图标设置——batchmode 保存 ProjectSettings 会让进程卡住并长期占住工程锁（本次两个工程路径因此被僵尸进程锁死）；③`forceSingleInstance=1`：多实例并发时 Unity player 会卡在图形初始化且无法终止（TerminateProcess/WMI/NtTask 全 ACCESS_DENIED），单实例保护从源头避免
- [x] 2026-09-12 求职收尾（V7 生命感版 + 门面包装，本轮）：`PetLifeMath`（呼吸/挤压弹簧含自动子步稳定性/冲击映射/倾角/帧率无关趋近，16 项测试）+ `PetLifeVisual` 表现层（静息呼吸、拖拽倾斜、落地挤压回弹、戳击反应；逻辑位置与渲染变换分层——物理永不读 transform，视觉装饰不污染模拟）；`SvgPetController` 升级为逻辑位置唯一真值 + 状态暴露 + 戳检测；首启引导 HUD（只显示一次）；应用图标（`tools/make_icon.py` 生成六尺寸 → `AppIconSetup` 设 exe 图标，托盘图标经 `LoadImageW` 从 exe 资源提取）；产品名 TransparentPet / BoranFan / 1.0.0；门面图工具 `ProductShots`（hero.png + breathe.gif + throw.gif，复用运行时同源物理与表现数学，"图 = 真实行为"）+ `tools/make_gif.py`；README 作品集化；80/80 测试通过；交付 exe 已复制到 `F:/Downloads/PetSpike/`
- [x] 2026-09-12 视觉锚定修复（亲自看图对比原版烘焙图）：密度场模糊 ×3 + iso 0.40（等值面脱离粒子层，坑洼根除）、s_corr 0.3（表层顺滑）、形状记忆 100=重力 15%（果冻蹲姿 ~84% 高度）且改全局权重（逐粒子权重破坏牛顿第三定律导致整团漂移，已修）、提亮三角色预设与着色器默认色（深色被透明背景透叠发黑）、BaseHalfWidth 88→80（对齐原版 160px）
- [x] 2026-09-12 重力常开语义（用户拍板）：世界=只有底部一个地面，重力常开松手即落；旧"落定悬浮"行为按版本保留约定做成 V2PbfHover 独立场景（非开关）
- [x] 2026-09-12 push 断链修复：35 笔本地提交推送到 GitHub，AGENTS 增加"commit 后立即 push"规则
- [x] 2026-09-12 软体重构（Subagent 并行）：贴图+刚体方案整体替换为 Verlet 粒子环+距离/辐射/面积压力三约束的压力软体（28 粒子，面积守恒），动态 Mesh+全程序化液态玻璃着色器（fwidth 抗锯齿根治像素阶梯、伪 3D 打光、色散伪折射、眨眼眼睛），撞墙面积转移式分裂/吸引融合，地面改 Windows 工作区（SPI_GETWORKAREA，根治落底被任务栏吃点击）——27/27 单测过，exe 已交付
- [x] 2026-09-12 产品化冲刺（Subagent 并行）：Slime.shader 动态着色器移植、EventBus+PetConfig JSON 持久化、多角色注册表（三色预设）、IMGUI 设置面板（缩放/角色/抛射参数/置顶/自启）、HUD 提示、托盘菜单泛化（设置/退出）、注册表开机自启、位置变化自动落盘——30/30 单测过，exe 已交付（该轮着色器/物理后被软体重构替换）
- [x] 2026-09-12 spike 窗口层实现：UniWinC v0.9.8 接入、ThrowPhysics/AlphaHitTestCore 移植 + 17 项测试、场景生成器、贴图烘焙（后两项已被软体方案替代）
- [x] 2026-09-12 参照库入库 external/：UniWindowController、AppWindowUtility、UnityDesktopPetFramework
- [x] 2026-09-12 小问题清理：README 打开路径笔误、PackageManagerSettings.asset 入库
- [x] 2026-09-19 运行期卡死三连修（事件日志实锤 4 次 AppHang/1002 后按外部审计收口）：①**运行期心跳看门狗**（CrashGuard 第二阶段——主线程每帧 Heartbeat 递增，停滞 10s 触发 5s 复核（防睡眠恢复竞态误杀），复核仍无心跳即 KillNow；任何主线程阻塞形态占屏上限 ~15s，配对进程由窗口信号联动收尾）；②**z 序配对去互锁**（PairZOrder 先用 GetWindow(GW_HWNDPREV) 纯查询比对，玻璃已在正上方就零跨进程调用——跨进程 SetWindowPos 是同步语义，对方不泵消息时本进程会被拖死成双窗口互锁）；③**全屏 BitBlt 抓屏挪工作线程**（~30fps 双缓冲发布、帧与尺寸成对交接，主线程只做 SetPixelData；DWM/驱动阻塞不再冻主循环，耗时 >100ms 限流警告日志可直接指认卡点）。同日先行修复退出链死锁：HardExit 弃 Environment.Exit（其 Mono runtime shutdown 挂起全部托管线程、连强杀引信一起冻住，实测联动退出日志打出后进程不死）+ 副进程存活改窗口存活信号双向联动（Process.Exited/HasExited 在 Unity Mono 下实测不可靠）+ 延迟 5s 拉起副进程防双进程 D3D 初始化竞争。106 项测试过；10.5 分钟长跑双进程 Responding=True 全程、1002 零新增；杀任一进程另一侧 4s 内联动退净；exe 已交付 F:/Downloads/TransparentPet/
- [x] 2026-09-19 最佳实践三连修（用户拍板"不是最优的部分也优化"）：①**Job Object 进程配对**——玻璃进程启动即入 KILL_ON_JOB_CLOSE 的 Job Object，子进程自动继承成员身份，父死 OS 即时杀全部子进程（实测强杀玻璃后 0.5s 内物种全灭，替代原 3~4s 窗口信号宽限路径；"子死父随"仍由窗口信号承担，两层互补）；踩坑：JOBOBJECT_BASIC_LIMIT_INFORMATION 的 Min/MaxWorkingSetSize 是 SIZE_T、Affinity 是 ULONG_PTR，声明成 uint 会 ERROR_BAD_LENGTH(24)。②**Desktop Duplication 主路径**——`NativeDesktopDuplication`（Platform）：ComImport 手写 DXGI/D3D11 互操作（全 IID/方法序对过 external/dxsdk 官方镜像——凭记忆写错了 5 处：Adapter1 尾段 1a05 非 1ffa、Output/Output1/Duplication/DeviceContext 四 IID、DeviceChild 继承链 4 方法、Output 链 Gamma×2+GetFrameStatistics；且 D3D_DRIVER_TYPE_UNKNOWN=0 非 4），全适配器扫描逐个试复制找 DWM 所在 GPU，Acquire 变化驱动等待，AccessLost（全屏切换/睡眠/锁屏）自动重建，行序翻转对齐 bottom-up BGRA 契约；先以独立验证台（dotnet 控制台）跑通再搬运。本机因虚拟显示器驱动污染枚举而不可用（见待办"环境整治"），自动回退 BitBlt 工作线程（日志可见路径切换；BitBlt 慢抓屏 110ms 警告实测触发，主循环无恙）。③**睡眠自愈**——duplication 的 AccessLost 重建 + BitBlt 无状态 + 心跳看门狗三层覆盖。106 项测试过；10 分钟长跑双进程 Responding=True、1002 零新增；杀玻璃 0.5s 全灭、杀物种 4s 联动退；exe 已交付 F:/Downloads/TransparentPet/
