# 待办事项

> 按 AGENTS 约定记录改进待办；做完的挪到底部归档区。

## 进行中

- [ ] **卡死修复的实机验证**：运行 `F:/Downloads/PetSpike/PetSpike.exe`，重点确认四件事——①**退出必死**（托盘"退出"/ESC 后进程立刻消失，不再需要任务管理器）；②**穿透正常**（鼠标移到宠物不透明区域上能抓；移到空白处点击落到桌面；托盘"设置"打开后**面板能点**）；③**无开场画面**（窗口在场景就绪前不出现，也不会整轮不出现）；④挂起是否复现（对照 `C:/ProgramData/Microsoft/Windows/WER/ReportArchive/AppHang_Pet*`，修复前 09-12 17:16~09-13 02:51 共 8 次）
- [ ] **启动画面屏蔽去留定夺**（需用户拍板）：7c073c0 引入、1cc4537 以"绕过副作用更大"回滚，但工作区又重新加回且已进构建产物（`SplashHider.cs` 一直是未提交状态）。本轮已把"藏起来就再也找不回来"的根因修掉（见归档），但**要不要保留这套屏蔽**需用户定：保留 = 无 Unity 开场标；放弃 = 接受个人版强制的开场标
- [ ] **V7 生命感版交付验收**：首次启动有 4 秒引导提示；静息会呼吸、拖动时身体倾斜、落地按冲击压扁回弹、戳一下会弹；托盘"设置/退出"、位置记忆照旧
- [ ] 快照对比存档：`C:/Users/fanbo/AppData/Local/Temp/pet-snapshot/combined_compare.png`（左原版/右 PBF，历史资料，不再更新）

## 排队中

- [ ] **卡死若仍复现，按"相位标记 + 心跳"定位**：在 `Pump`/`TrackPopupMenu`、`Shell_NotifyIcon` 前后写带 flush 的日志，外加后台心跳线程（>2 秒无帧就记下最后一个相位），用 `-logFile -` 跑。本轮排查吃过"Player.log 带缓冲、强杀时尾部全丢"的亏（见归档教训）
- [ ] 主线程停转检测：把 `CrashGuard` 的看门狗从"仅启动期"扩展到"运行期"（现只覆盖启动 30 秒），运行中主线程 >N 秒无帧时记日志或强退，避免再次出现"占屏且只能任务管理器"
- [ ] 托盘菜单 Win32 细节收口：菜单宿主仍是 `HWND_MESSAGE` 消息窗口（写过探针实测 `SetForegroundWindow` 能成功，故未改），但 `TrackPopupMenu` 期间主循环停摆属固有行为——可考虑改为非模态自绘菜单
- [ ] **窗口跟随宠物包围盒**（取代全屏透明覆盖层）：把失败影响面从"遮挡整个桌面"降到"一小块"；代价是窗口需随宠物移动（SetWindowPos）与多显示器处理
- [ ] 更早版本的场景化保留（V1 贴图刚体版 / V1.5 轮廓软体版——代码在 git 历史，需按 Versions/ 约定 resurrect 成独立场景）
- [ ] 分裂/融合（PBF 物理断开 + 聚类回流，参照 Unity_Slime 连通域方案；作为新版本场景做）
- [ ] **V8 液态玻璃实机验证**：编辑器打开 `V8LiquidGlass/PetScene.unity` 或构建验证——玻璃史莱姆可拖拽、玻璃外穿透正常（CPU SDF 命中 → PointerHover）、拖拽位置记忆、全屏分辨率下折射带宽是否需要随分辨率重标定（当前参数按 800×560 快照调的）
- [ ] 液态玻璃进阶（可选）：桌面背景抓屏（BitBlt/Desktop Duplication，Core 层新增抓屏能力）让玻璃折射真实桌面；多只玻璃史莱姆 smin 融合同屏（shader 已预留 3 槽位）
- [ ] 音效（低优先）
- [ ] 编辑器 Play 体验（Game 视图切换/黑底问题，不影响 exe 交付链路）
- [ ] 多显示器支持验证（shouldFitMonitor 只适配主屏）
- [ ] `Assets/Scenes/Versions/` 与 `ProjectSettings/EditorBuildSettings.asset` 路径不一致：后者仍写着 `V2SplitFusion`/`V9PbfHover` 等已不存在的路径（重跑场景生成器即可对齐）
- [ ] `Assets/Scripts/Display/` 与构建产物里的 `TransparentPet.Display.dll` 来历未明（源码树里没有对应的 asmdef 与其源文件），需清理或补文档
- [ ] 首个正式版打包时把工程 Temp/ 加入 .gitignore 或清理（历史上出现过 Unity 僵尸进程持锁导致无法构建，处置：复制工程到新路径构建）

## 归档

- [x] 2026-09-16 V8 液态玻璃版（把 Godot 版液态玻璃演示学过来做成史莱姆形状）：三个 Built-in shader 移植 `modules/effects/`（`LiquidGlassBg` 程序化素材 / `LiquidGlassBlur` 分离式双 pass 高斯 / `LiquidGlass` 主合成：SDF→斯涅尔折射→RGB 色散→LCH 菲涅尔/眩光→抗锯齿）；`LiquidGlassController` 用 RenderTexture+Blit 复刻 Godot SubViewport 管线，可拖拽+位置记忆；命中判定用 `LiquidGlassSlimeSdf`（与 shader 同源贝塞尔控制点，9 项测试）→ PointerHover 悬停自报，玻璃外穿透不挡桌面；棋盘格素材只在玻璃轮廓内可见（适配全屏透明覆盖层）。**踩坑记录**：①ShaderLab Properties 的 `[Header()]` 不容 `&`；②shader 单标量向量构造 `float3(x)`/标量 swizzle `1e-6.xx` 编译期才报错（D3D 编译 lazy，batchmode compile 验证不触发，必须实际渲染）；③编辑器非 Play 下 AddComponent 不触发 Awake，无头快照需幂等 `EnsureInitialized()`；④Godot 4 屏幕坐标 y 向下，sdSlime 平底在 +0.275 端，Unity GL 语义直接用会上下颠倒——SDF 空间统一 y 向下后与 CPU 判定同源。新增 `LiquidGlassSnapshot.CaptureHeadless`（主渲染×2 素材+SDF+法线四图人工验收）；86/86 测试过

- [x] 2026-09-13 卡死四路收口（用户报告"冷不丁还是会卡死"；Windows 事件日志确认 09-12 17:16~09-13 02:51 共 8 次 `AppHangB1`/`AppHangXProcB1`，均判为"主线程在等本进程之外的东西"）：①**退出路径**——`ExitFromTray` 原本先 `Shell_NotifyIcon`（`NIM_DELETE`，同步跨进程调用任务栏）摘图标再 `KillNow`，对方不响应就永不返回、带着全屏置顶窗口赖死（这正是历史上"点了退出进程还赖着不死"的成因，`KillNow` 兜不住"卡在 KillNow 之前"），新增 `HardExit.KillSoon` 延迟强杀引信并在 `Now()`/托盘退出开头点燃；②**去掉每帧 GPU 回读**——`hitTestType` 由 `Opacity` 改 `None` + `isHitTestEnabled=false`（UniWinC 的读屏协程不检查 isHitTestEnabled，只能靠改 hitTestType 关掉），穿透改由新增的 `PointerHover`（Core，帧号显式传入、容忍滞后一帧、6 项单测）驱动，上报方 = 四个宠物控制器（Svg 贴图 alpha / Pbf·Mesh·Split 的 `ContainsPoint`）+ `SettingsPanel`（IMGUI 矩形；其注释原本明确依赖"鼠标落在面板上变可交互"，不自报就会点不动）；③**托盘菜单动作**从窗口过程/`DispatchMessageW` 嵌套栈挪到 `Pump` 顶层执行；④**`FindCurrentProcessTopLevelWindow` 不再单向按可见性过滤**（新增 `requireVisible` 分档 + Unity 窗口类名精确匹配），启动屏蔽"藏起来→再枚举找不到→永远不显示"的根因修掉，隐藏/恢复改为 `HideMainWindowForSplash`/`ReleaseMainWindow` 成对配对（含"藏晚了"竞态保护）。**排查方法教训**：Player.log 带缓冲，进程被强杀时尾部全丢（`Player-prev.log` 只剩 34 行停在正常日志中间），日志尾部毫无线索；真正的证据是 Windows 事件日志（`Application Hang` 1002 + WER `AppHangXProcB1`）与 `crashguard.log` 的启动次数/时间对比（每次挂起后 30 秒~3 分钟必有一次新启动 = 任务管理器杀掉后重开）。77/77 测试通过，exe 已交付 `F:/Downloads/PetSpike/`

- [x] 2026-09-12 失控保护加固（用户反馈"一失控就跟电脑病毒似的"之后）：①**移除构建产物里的崩溃处理器**（`UnityCrashHandler64.exe`）——这是"进程杀不掉"的根因（崩溃时 CrashHandler 挂起进程收集信息导致 TerminateProcess/WMI/NtNativeTerminate 全部 ACCESS_DENIED），移除后实测任务管理器可正常结束进程；②新增 `CrashGuard`（Core）：命名互斥体单实例保护（Unity 的 `forceSingleInstance` 实测无效）+ 独立后台线程启动看门狗（30 秒未进入渲染循环即硬退出，主线程卡死时协程/Update 全停摆，只有独立线程能救）+ AppDomain 未处理异常兜底；③实测：稳定实例下二次启动自动退出（count 保持 1），`crashguard.log` 留痕；已知限制：两次启动几乎同时时，后启动进程可能卡在引擎初始化（应用代码尚未运行，无法拦截），但可被正常终止
- [x] 2026-09-12 构建流程加固（本轮实测踩坑后收口）：①应用图标改由**构建后注入 PE 资源**（`AppIconSetup.InjectIntoExe`：RT_ICON + RT_GROUP_ICON，验证过提取正确）——Unity 的 `PlayerSettings.SetIcons` 在 batchmode 下不落盘（ProjectSettings 恒为空）；②`SceneGenerator` 不再调用 PlayerSettings 图标设置——batchmode 保存 ProjectSettings 会让进程卡住并长期占住工程锁（本次两个工程路径因此被僵尸进程锁死）；③`forceSingleInstance=1`：多实例并发时 Unity player 会卡在图形初始化且无法终止（TerminateProcess/WMI/NtTask 全 ACCESS_DENIED），单实例保护从源头避免
- [x] 2026-09-12 求职收尾（V7 生命感版 + 门面包装，本轮）：`PetLifeMath`（呼吸/挤压弹簧含自动子步稳定性/冲击映射/倾角/帧率无关趋近，16 项测试）+ `PetLifeVisual` 表现层（静息呼吸、拖拽倾斜、落地挤压回弹、戳击反应；逻辑位置与渲染变换分层——物理永不读 transform，视觉装饰不污染模拟）；`SvgPetController` 升级为逻辑位置唯一真值 + 状态暴露 + 戳检测；首启引导 HUD（只显示一次）；应用图标（`tools/make_icon.py` 生成六尺寸 → `AppIconSetup` 设 exe 图标，托盘图标经 `LoadImageW` 从 exe 资源提取）；产品名 TransparentPet / BoranFan / 1.0.0；门面图工具 `ProductShots`（hero.png + breathe.gif + throw.gif，复用运行时同源物理与表现数学，"图 = 真实行为"）+ `tools/make_gif.py`；README 作品集化；80/80 测试通过；交付 exe 已复制到 `F:/Downloads/PetSpike/`
- [x] 2026-09-12 视觉锚定修复（亲自看图对比原版烘焙图）：密度场模糊 ×3 + iso 0.40（等值面脱离粒子层，坑洼根除）、s_corr 0.3（表层顺滑）、形状记忆 100=重力 15%（果冻蹲姿 ~84% 高度）且改全局权重（逐粒子权重破坏牛顿第三定律导致整团漂移，已修）、提亮三角色预设与着色器默认色（深色被透明背景透叠发黑）、BaseHalfWidth 88→80（对齐原版 160px）
- [x] 2026-09-12 重力常开语义（用户拍板）：世界=只有底部一个地面，重力常开松手即落；旧"落定悬浮"行为按版本保留约定做成 V2PbfHover 独立场景（非开关）
- [x] 2026-09-12 push 断链修复：35 笔本地提交推送到 GitHub，AGENTS 增加"commit 后立即 push"规则
- [x] 2026-09-12 软体重构（Subagent 并行）：贴图+刚体方案整体替换为 Verlet 粒子环+距离/辐射/面积压力三约束的压力软体（28 粒子，面积守恒），动态 Mesh+全程序化液态玻璃着色器（fwidth 抗锯齿根治像素阶梯、伪 3D 打光、色散伪折射、眨眼眼睛），撞墙面积转移式分裂/吸引融合，地面改 Windows 工作区（SPI_GETWORKAREA，根治落底被任务栏吃点击）——27/27 单测过，exe 已交付
- [x] 2026-09-12 产品化冲刺（Subagent 并行）：Slime.shader 动态着色器移植、EventBus+PetConfig JSON 持久化、多角色注册表（三色预设）、IMGUI 设置面板（缩放/角色/抛射参数/置顶/自启）、HUD 提示、托盘菜单泛化（设置/退出）、注册表开机自启、位置变化自动落盘——30/30 单测过，exe 已交付（该轮着色器/物理后被软体重构替换）
- [x] 2026-09-12 spike 窗口层实现：UniWinC v0.9.8 接入、ThrowPhysics/AlphaHitTestCore 移植 + 17 项测试、场景生成器、贴图烘焙（后两项已被软体方案替代）
- [x] 2026-09-12 参照库入库 external/：UniWindowController、AppWindowUtility、UnityDesktopPetFramework
- [x] 2026-09-12 小问题清理：README 打开路径笔误、PackageManagerSettings.asset 入库
