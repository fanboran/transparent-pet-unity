# 待办事项

> 按 AGENTS 约定记录改进待办；做完的挪到底部归档区。

## 进行中

- [ ] **spike 真机验收**：Windows Build 出 exe，过一遍 `docs/spike-透明窗口.md` 六项清单，记录帧率数字，回填结论（Play 模式可快速看效果，但 UniWinC 会操纵编辑器窗口，正式验收以 build 为准）

## 排队中（spike 通过后）

- [ ] 系统托盘（Godot 版用 addons/system_tray；Unity 侧调研现成方案或自己包 Shell_NotifyIcon）
- [ ] 右键菜单 / 设置窗口（对应 Godot 版 settings 窗口：缩放、材质、抛射参数）
- [ ] 史莱姆动态着色器：slime.gdshader → Shader Graph（顶点晃动 + FBM 流动 + 菲涅尔），当前是静态近似贴图
- [ ] 宠物位置持久化（对应 Godot DataManager 的 pet_position）
- [ ] 观察任务栏隐藏的健壮性：UniWinC 切换样式可能重置 WS_EX_TOOLWINDOW，必要时改挂它的 OnStateChanged 事件重打
- [ ] （低优）安全网分支在地面夹紧顺序下不可达，与 Godot 行为一致保留；重构时再决定去留
- [ ] （低优）接地微幅反弹永续、无静止判定——与 Godot 一致，若视觉可见抖动再优化

## 归档

- [x] 2026-09-12 spike 窗口层实现：UniWinC v0.9.8 接入、ThrowPhysics/AlphaHitTestCore 移植 + 17 项测试全过、场景生成器、贴图烘焙（Godot SVG 路径 + slime.gdshader 配色）
- [x] 2026-09-12 参照库入库 external/：UniWindowController、AppWindowUtility、UnityDesktopPetFramework
- [x] 2026-09-12 小问题清理：README 打开路径笔误、PackageManagerSettings.asset 入库
